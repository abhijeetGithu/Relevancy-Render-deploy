# Server Configuration
PORT=5050
HOST=0.0.0.0
FLASK_DEBUG=false

# Docker / Gunicorn Configuration
GUNICORN_WORKERS=2
GUNICORN_TIMEOUT=1200
MEMORY=2G
EXPOSED_PORT=5050
IMAGE=relevancy-evaluator:latest

# Optional: Default API keys (users should set their own in the UI)
# OPENAI_API_KEY=
# GEMINI_API_KEY=